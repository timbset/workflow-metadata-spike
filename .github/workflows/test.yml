name: test.yml

on:
  push:
    branches:
      - '**/*'
  pull_request:
    branches:
      - '**/*'

jobs:
  first: 
    runs-on: ubuntu-slim
    steps:
      - name: Reads workflow metadata
        run: echo {{ github }}
