name: test.yml

on:
  push:
    branches:
      - '**/*'
  pull_request:
    branches:
      - '**/*'

jobs:
  first: 
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Reads workflow metadata
        run: echo yq '.' ${{ github.workspace }}/.github/workflows-metadata.yml
